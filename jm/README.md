terraform init
terraform plan
terraform apply --auto-approve


tflocal init
tflocal plan
tflocal apply --auto-approve
dig @dns.localhost.localstack.cloud test.hellochaos.com

awslocal fis list-experiment-templates
